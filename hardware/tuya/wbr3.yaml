rtl87xx:
  board: wbr3

logger:
  baud_rate: 0

uart:
  tx_pin: PA14
  rx_pin: PA13
  baud_rate: 9600

tuya:

packages:
  # Default common config
  default: !include ../../common/default.yaml
  # HTTP server config
  web: !include ../../common/web.yaml
