rtl87xx:
  board: generic-rtl8720cf-2mb-896k # wbr3 not supported yet, but this is close enough

logger:
  baud_rate: 0

uart:
  tx_pin: PA14
  rx_pin: PA13
  baud_rate: 9600

tuya:

packages:
  # Default common config
  default: !include ../../common/default.yaml
  # HTTP server config
  web: !include ../../common/web.yaml
